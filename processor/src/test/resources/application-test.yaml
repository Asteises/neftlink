jdbc:
  driverClassName: org.h2.Driver
  url: jdbc:h2:mem:db
  username: sa
  password: sa
spring:
  jpa:
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: always
      data-locations:
  datasource:
    initialization-mode: always

qwe:
  asd: here